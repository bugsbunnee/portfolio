import { Bebas_Neue, Caveat, Lato } from "next/font/google";

export const bebasNeue = Bebas_Neue({ 
    display: "swap",
    subsets: ['latin'], 
    weight: ['400'],
    variable: '--font-bebas-neue'
});

export const caveat = Caveat({ 
    display: "swap",
    subsets: ['latin'], 
    weight: ['400'],
    variable: '--font-caveat'
});
